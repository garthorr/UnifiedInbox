services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: unified_inbox
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "psql -U ${POSTGRES_USER} -d unified_inbox -c 'SELECT 1' > /dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s

  migrate:
    build:
      context: .
      target: migrate
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/unified_inbox
    networks:
      - internal

  web:
    build:
      context: .
      target: production
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    env_file: .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/unified_inbox
    networks:
      - internal
      - traefik
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.unified-inbox.rule=Host(`inbox.home.tastymath.com`)"
      - "traefik.http.routers.unified-inbox.entrypoints=websecure"
      - "traefik.http.routers.unified-inbox.tls=true"
      - "traefik.http.routers.unified-inbox.tls.certresolver=le"
      - "traefik.http.services.unified-inbox.loadbalancer.server.port=3000"

  worker:
    build:
      context: .
      target: worker-production
    depends_on:
      db:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    env_file: .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/unified_inbox
    networks:
      - internal
    restart: unless-stopped

networks:
  internal:
  traefik:
    external: true
    name: traefik
